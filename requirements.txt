# ---------------------------------------------------------------------------
# ATM Cash Balance Forecasting  --  Python dependencies
# Tested with Python 3.10 / 3.11
# ---------------------------------------------------------------------------

# ----- Core scientific stack -----
numpy>=1.26,<2.1
pandas>=2.1,<2.3
scipy>=1.11

# ----- Classical time-series models -----
statsmodels>=0.14          # SARIMA / SARIMAX, Holt-Winters, stationarity tests
pmdarima>=2.0              # auto_arima order selection
prophet>=1.1               # Facebook/Meta Prophet

# ----- Machine-learning / deep-learning models -----
scikit-learn>=1.3
tensorflow>=2.15           # LSTM sequence model (CPU build is fine)

# ----- Visualisation -----
matplotlib>=3.8
seaborn>=0.13

# ----- RAG / LLM layer (mirrors the FinApp RAG sample) -----
langchain>=0.2
langchain-google-genai>=1.0   # Google Gemini via LangChain
langchain-community>=0.2
chromadb>=0.5                 # vector store
google-generativeai>=0.7      # Gemini SDK
pypdf>=4.0                    # PDF report ingestion
tiktoken>=0.7

# ----- Serving layer -----
fastapi>=0.111
uvicorn[standard]>=0.30
sse-starlette>=2.1            # server-sent events / streaming
python-multipart>=0.0.9
pydantic>=2.6
pydantic-settings>=2.2

# ----- Utilities -----
pyyaml>=6.0
python-dotenv>=1.0
holidays>=0.50               # national holiday calendars (India, etc.)
rich>=13.7                   # pretty CLI output
typer>=0.12                  # CLI framework

# ----- Testing / quality (dev) -----
pytest>=8.0
pytest-cov>=5.0

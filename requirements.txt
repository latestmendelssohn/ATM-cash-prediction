# The pure-Python core (data.py, models.py) needs NOTHING here: it runs on the
# standard library alone. These packages enable SARIMA, the RAG analyst and the API.
# Versions are pinned so a checkout months from now installs what was tested.

# Classical time-series (SARIMA). 0.14.6 or newer: earlier releases call
# scipy's fmin_l_bfgs_b with a `disp` argument that recent scipy removed.
statsmodels==0.14.6

# RAG analyst layer
langchain-google-genai==2.0.8    # Gemini chat + embeddings via LangChain
chromadb==1.5.9                  # vector store (0.5.x needs a C++ compiler on Windows)
pypdf==5.1.0                     # PDF ingestion

# API
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20        # file uploads
python-dotenv==1.0.1

# Public Streamlit demo
streamlit==1.50.0

# Dev / tests
pytest==8.3.5

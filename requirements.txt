# The pure-Python core (data.py, models.py) needs NOTHING here — it runs on the
# standard library alone. These packages enable SARIMA, the RAG analyst and the API.

# Classical time-series (SARIMA)
statsmodels>=0.14

# RAG analyst layer (mirrors the FinApp RAG sample)
langchain-google-genai>=1.0     # Gemini chat + embeddings via LangChain
chromadb>=0.5                   # vector store
pypdf>=4.0                      # PDF ingestion

# API
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9         # file uploads
python-dotenv>=1.0

# Dev / tests
pytest>=8.0
